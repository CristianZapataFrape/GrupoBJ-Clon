@model IEnumerable<GrupoBJ.Models.ClienteCls>
@if (Model.Count() > 0)
{
@foreach (var item in Model)
{
<tr>
    @*<td>@Html.DisplayFor(m => item.codigo)</td>
    <td>@Html.DisplayFor(m => item.nombre)</td>*@
        @*<td >@Html.DisplayFor(m => item.codigo)</td>*@
        <td hidden class="Tcln-id_Cliente">@Html.DisplayFor(m => item.id_Cliente)</td>
        <td hidden class="Tcln-nombre">@Html.DisplayFor(m => item.Nombre)</td>
        <td hidden class="Tcln-telefono">@Html.DisplayFor(m => item.Telefono)</td>
        <td hidden class="Tcln-correo">@Html.DisplayFor(m => item.Email)</td>
        <td hidden class="Tcln-cp">@Html.DisplayFor(m => item.CP)</td>
</tr>} }
            else
            {
<br />
                <p>No hay registros</p>}